<h2 class="articles">Check Out Today's Articles!</h2>
      <hr>
     		  
  <div class ="row">  
    {{#each articles }}
      <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3 articleDiv">
				<h2 id="articleName"> {{ title }} </h2>
				<div> 
					<a href="{{ link }}" id="articleLink"> Read More </a>
				</div>
      </div>
    {{/each}}
  </div>